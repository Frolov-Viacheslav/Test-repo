pipeline {
  agent any
  stages {
    stage('') {
      steps {
        println "${env.BRANCH_NAME}"
        if(${env.BRANCH_NAME} == "main"){
          echo "Main branch"
        }
      }
    }

  }
}
